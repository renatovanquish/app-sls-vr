import s from './Loading.module.css'

const Loading: React.FC = () => {
  return (
    <span className={s.root}>
      <span />
      <span />
      <span />
    </span>
  )
}

export default Loading
